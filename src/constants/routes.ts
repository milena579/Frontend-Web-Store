export enum ROUTES {
    home = "/",
    cart = "/carrinho",
    products = "/produtos",
    login = "/login",
    profile = "/profile",
    admCategories = "/admCategories",
    admProducts = "/admProducts"

}